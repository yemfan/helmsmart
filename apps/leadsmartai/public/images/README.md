# Static images

- **`lslogoh.png`** — legacy header/footer logo. Its only consumer, `LeadSmartLogo`, was removed along with the unused `Header`; the live wordmark is `components/brand/CloseBossLogo.tsx`.
- **`lslog64.png`** — 64×64 favicon (same pixels as `ls64.png`); copied to **`app/icon.png`**.
- **`ls180.png`** — 180×180 Apple touch icon (`metadata.icons.apple`).
- **`ptlogo.png`** / **`ptlog.png`** / **`pt-logo64.png`** / **`pt-logo180.png`** — optional Property Tools variants.
