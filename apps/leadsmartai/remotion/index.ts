import { registerRoot } from "remotion";

import { RemotionRoot } from "./Root";

// Remotion entry point — `remotion studio remotion/index.ts` (preview) and
// `remotion render` / renderMediaOnLambda both start here.
registerRoot(RemotionRoot);
